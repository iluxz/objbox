[package]
name = "signal-native"
version = "0.1.0"
edition = "2021"
description = "A native egui shell for the Signal Detected cube experience"
license = "MIT"

[dependencies]
eframe = "0.29"
egui = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
